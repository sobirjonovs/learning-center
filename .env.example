# Lokal ishlab chiqish
DATABASE_URL="file:./dev.db"

# ---- Server (VPS) sozlamalari ----
# DATABASE_URL="file:/var/lib/learning-center/data.db"
# NODE_ENV="production"
# Papkani yarating: sudo mkdir -p /var/lib/learning-center && sudo chown $USER /var/lib/learning-center
